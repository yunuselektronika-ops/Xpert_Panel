# ⚡ Xpert Panel

**VPN Subscription Aggregator** - система агрегации VPN подписок с автоматической проверкой пинга и задержки.

## 🚀 Возможности

- ➕ Добавление ссылок на подписки-источники через веб-интерфейс
- 🔍 Автоматическая проверка пинга и задержки каждого конфига
- ✅ Фильтрация только рабочих конфигураций
- 🔄 Автообновление подписки каждый час
- 📱 Поддержка форматов: Universal, Base64, Clash, Surge, Happ
- 🌐 Раздача через один домен и порт

## 📋 Требования

- Python 3.11+
- Docker (опционально)

## 🛠 Установка

### Локальный запуск

```bash
# Клонирование
git clone https://github.com/yunuselektronika-ops/Xpert_Panel.git
cd Xpert_Panel

# Установка зависимостей
pip install -r requirements.txt

# Запуск
python run.py
```

### Docker

```bash
# Сборка и запуск
docker-compose up -d

# Или через Docker напрямую
docker build -t xpert-panel .
docker run -d -p 8000:8000 -v ./data:/app/data xpert-panel
```

## ⚙️ Конфигурация

Переменные окружения:

| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| `HOST` | Хост сервера | `0.0.0.0` |
| `PORT` | Порт сервера | `8000` |
| `DOMAIN` | Домен для подписки | `home.turkmendili.ru` |
| `UPDATE_INTERVAL` | Интервал обновления (часы) | `1` |
| `MAX_PING_MS` | Максимальный пинг (мс) | `300` |
| `MAX_CONFIGS` | Максимум конфигов в подписке | `100` |

## 🔗 Эндпоинты

| URL | Описание |
|-----|----------|
| `/` | Админ панель |
| `/sub` | Подписка (universal формат) |
| `/sub?format=base64` | Подписка в Base64 |
| `/sub?format=clash` | Подписка для Clash |
| `/sub?format=happ` | Подписка для Happ |
| `/health` | Health check |

## 📱 Использование в приложениях

### Happ / Proxy Utility

1. Откройте приложение
2. Добавить подписку → Ввести URL
3. URL: `https://home.turkmendili.ru/sub?format=happ`
4. Установите автообновление: **1 час**

### Shadowrocket

1. Настройки → Подписки → Добавить
2. URL: `https://home.turkmendili.ru/sub?format=base64`

### Clash

1. Профили → Добавить
2. URL: `https://home.turkmendili.ru/sub?format=clash`

## 📁 Структура проекта

```
Xpert_Panel/
├── app/
│   ├── __init__.py
│   ├── config.py      # Конфигурация
│   ├── models.py      # Модели данных
│   ├── storage.py     # Хранилище данных
│   ├── checker.py     # Проверка конфигов
│   ├── generator.py   # Генератор подписок
│   ├── scheduler.py   # Планировщик обновлений
│   └── main.py        # FastAPI приложение
├── data/              # Данные (создается автоматически)
├── requirements.txt
├── Dockerfile
├── docker-compose.yml
├── run.py
└── README.md
```

## 🔧 API

### Источники

- `GET /api/sources` - Список источников
- `POST /api/sources` - Добавить источник
- `DELETE /api/sources/{id}` - Удалить источник
- `POST /api/sources/{id}/toggle` - Вкл/Выкл источник

### Конфигурации

- `GET /api/configs` - Список активных конфигов
- `POST /api/update` - Принудительное обновление

### Статистика

- `GET /api/stats` - Статистика системы
- `GET /api/scheduler` - Статус планировщика

## 📄 Лицензия

MIT License

---

**Xpert Panel** v1.0.0 | Домен: `home.turkmendili.ru`
